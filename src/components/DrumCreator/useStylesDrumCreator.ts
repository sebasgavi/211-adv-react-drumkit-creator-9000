import { createStyles, makeStyles, Theme } from "@material-ui/core";

export const useStylesDrumCreator = makeStyles((theme: Theme) =>
  createStyles({
    root: {
      border: '3px solid red',
    },
    otra: {
      
    }
  })
);
